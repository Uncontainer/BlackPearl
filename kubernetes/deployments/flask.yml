apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: flask
  namespace: test
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: flask
      name: flask
    spec:
      containers:
      - name: flask
        image: redcarpet/redcarpet-flask
        imagePullPolicy: Never
        ports: 
          - containerPort: 9001
            name: flask-uwsgi